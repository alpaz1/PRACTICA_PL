principal() {
    entero n = 1;
    mientras (n < 10) {
        selecciona(n){
            caso 5:
                imprime(500);
                ¡para!;
            caso 3:
                imprime(300);
            porDefecto:
                imprime(n);
        }
        n = n + 1;
    }
    
}